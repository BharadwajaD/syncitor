# has map of user_id, pid
# has no context of user_id and group it belongs to 
defmodule TcpServer.TcpServer do
  require Logger

  def new do
  end

  # TODO: Correct these
  def send(tcp_pool, user_id, message) do
  end
end
